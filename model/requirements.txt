# ==============================================================================
# PHARMABOT PH — Python Dependencies
# ==============================================================================
# Optimized for RTX 4050 (6 GB VRAM) with QLoRA 4-bit training.
# All packages are CPU/GPU compatible and installable via pip.
# ==============================================================================

# --- Core Deep Learning ---
torch>=2.0
torchvision
torchaudio

# --- Hugging Face Ecosystem ---
transformers>=4.40.0
accelerate>=0.29.0
datasets>=2.18.0
peft>=0.10.0
bitsandbytes>=0.43.0        # Required for 4-bit QLoRA on 6 GB VRAM

# --- Classical ML / NLP ---
scikit-learn>=1.4.0
nltk>=3.8.1
gensim>=4.3.2               # Word2Vec training (course requirement)

# --- Information Retrieval ---
rank-bm25>=0.2.2

# --- Data & Utilities ---
pandas>=2.2.0
numpy>=1.26.0
tqdm>=4.66.0
joblib>=1.4.0
matplotlib>=3.8.0           # For evaluation plots in notebooks
seaborn>=0.13.0               # For prettier classifier evaluation heatmaps

# --- Jupyter (for notebooks) ---
ipykernel>=6.29.0
ipython>=8.22.0

# --- Web UI & API ---
gradio>=4.0.0               # Notebook interactive UI
flask>=3.0.0                # REST API
flask-cors>=5.0.0           # CORS for API

# --- Text Utilities ---
unidecode>=1.3.0            # Unicode normalization
